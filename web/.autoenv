# for use with https://github.com/hyperupcall/autoenv
# be sure to set export AUTOENV_ENV_FILENAME=.autoenv in your .bashrc
source "$AUTOENV_CUR_DIR"/venv/bin/activate
eval "$(invoke --print-completion-script=bash)"
